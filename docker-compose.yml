services:
  nora:
    build: .
    container_name: nora
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./personas:/app/personas:ro
      - ./data:/app/data
# Need for my local deployment
#
#    networks:
#      - amiya
#
#networks:
#  amiya:
#    external: true
